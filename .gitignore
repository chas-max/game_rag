.env
__pycache__/
*.pyc

# IDE
.idea/
.claude/

# 评测指标日志(本地 append-only 产物,避免多人冲突;测试集 benchmark.json 需提交以保证版本对比一致)
eval/*.xlsx

# ChromaDB 向量库持久化(可由 scripts/migrate_to_chroma.py 从 SQLite 重建)
data/chroma/

# SQLite WAL 文件(运行时临时文件)
data/*.db-shm
data/*.db-wal

# SQLite 数据库(运行时数据,含本地测试对话,可由 init_db 重建;勿提交)
data/*.db

# 测试产物(浏览器 MCP 快照、截图)
.playwright-mcp/
current_page.png